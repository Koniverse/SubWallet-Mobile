import Link, { LinkProps } from './Link';
import Text, { TextProps } from './Text';
import Title, { TitleProps } from './Title';

export type { LevelProps, SuperLevelProps, TextSizeProps } from './PropsType';
export type { LinkProps, TextProps, TitleProps };
const Typography = {
  Text,
  Title,
  Link,
};

export default Typography;
